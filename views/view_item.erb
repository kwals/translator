<!-- This is where you can look at a particular item and read comments and comment if you would like -->
<html>
<head> 
  <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" rel="stylesheet">
  <title>Item</title>
</head>
<body>
  <div class="container">
    <div class="row"id="Item">
      <% item = Item.find(@id) %>
      <h3><%=item.title%></h3>
      <p><%=item.original_text%></p>
      <p><%=item.translated_text%></p>
      <p><%=item.user_content%></p>>
    </div>
    <% comments = item.comments.all %>
    <%comments.each do |comment|%>
    <div class="row" id="comment"> 
    <p><%=comment.content%></p>
    </div> 
    <%end%>
  </div>
  </div
</body>
</html>